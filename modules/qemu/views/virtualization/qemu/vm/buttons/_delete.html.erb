            <%=
              action_button(
                :delete,
                :name     => 'delete[uuid]',
                :title    => 'Delete!',
                :value    => vm.uuid,
                :disabled => (vm.running? or vm.snapshotting?)
              )
            %>


