CREATE DATABASE `radius` DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci;

GRANT ALL PRIVILEGES ON `radius`.* TO 'radius'@'%';

SET PASSWORD FOR 'radius'@'localhost' = PASSWORD( 'radius' ); -- Change to the actual password.
