<% 
  user    = objects['user']
  conf    = objects['conf']
  chkcols = conf['user']['check']['columns']
  rplcols = conf['user']['reply']['columns'] 
%>
<%= message_partial(msg) %>
<%=
  partial(
    :module => 'radius-admin',
    :path => 'services/radius/users/_form_style'
  )
%>

<h2>RADIUS user: <%= user.name %></h2>

<%= 
  partial(
    :module => 'radius-admin',
    :path   => 'services/radius/users/_form_edit',
    :locals => {
      :user     => user,
      :heading  => 3
    }
  )
%>

<form method="POST">
  <input type="hidden" name="_method" value="put"/>
  <h3>Groups</h3>
  <p>
    Comma-separated list, in order of priority e.g. <code>group1, 
    group2</code> . You may also add non existing groups, they will be 
    created for you.
  </p>

  <p><input type="text" style="width:50ex" name="groups" value="<%= user.grouplist.join(', ') %>"/></p> 
  <div style="margin-top:2em;"><button type="submit" name="update_groups" value="on">Update group membership!</button></div>
</form>


