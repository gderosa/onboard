Due to a bug or a misconfiguration at compile time, you may need to symlink:

ln -s /var /usr/local/var (or whatever the install prefix is)

if you compile from source (coova-chilli-1.2.3),

even if you correctly use absolute paths in config files.




